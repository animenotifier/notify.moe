section
	header
		h2 Users
	
	p
		a.ajax(href='/users') All at once
		|  | 
		a.ajax(href='/users/listproviders') List providers
		|  | 
		a.ajax(href='/users/registration') Registration date
	
	each category, categoryTitle in categories
		h3(title=category.length + (category.length === 1 ? ' user' : ' users'))= categoryTitle
		each user in category
			.user
				a.ajax(href='/+' + user.nick, title=user.nick)
					img.user-image(src=user.gravatarURL, alt=user.nick)